spring:
  application:
    name: Mini Drive Storage API

  datasource:
    url: jdbc:postgresql://localhost:5432/minidrive_db
    username: minidrive_user
    password: 1

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

file:
  upload-dir: uploads
